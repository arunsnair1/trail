package com.caladapt.domain.model

/**
 * Biological sex used for BMR calculation (Mifflin-St Jeor formula).
 * This is NOT gender — it is strictly the metabolic variable.
 */
enum class Sex {
    MALE,
    FEMALE
}
